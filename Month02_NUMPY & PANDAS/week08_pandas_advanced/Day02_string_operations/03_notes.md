df.dtypes → data types

astype(int/float/str) → change type

pd.to_numeric(col, errors="coerce")
→ invalid values become NaN

.str.lower() → lowercase
.str.upper() → uppercase
.str.strip() → remove outer spaces

.str.contains("text", case=False)
→ search text

.str.replace("old","new")
→ replace text