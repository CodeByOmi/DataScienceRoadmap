# Week 6 Day 2

split()
Splits one array into multiple arrays.

hsplit()
Splits horizontally (columns).

vsplit()
Splits vertically (rows).

view()
Shares memory with original array.

copy()
Creates a completely independent array.

Broadcasting
Allows operations between arrays of compatible shapes without loops.