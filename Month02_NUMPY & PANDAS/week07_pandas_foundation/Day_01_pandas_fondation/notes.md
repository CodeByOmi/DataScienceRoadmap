Pandas → table/data analysis library

Series → 1D labeled data / one column

DataFrame → 2D table

Index → identifies rows

head() → first 5 rows

tail() → last 5 rows

shape → (rows, columns)

shape is an attribute, not a function