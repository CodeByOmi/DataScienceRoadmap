df[df["Age"] > 25] → filter rows

& → AND
| → OR
~ → NOT

isin([...]) → match multiple values
between(a,b) → filter a range

sort_values("col") → ascending
sort_values("col", ascending=False) → descending

df["New"] = calculation → create column